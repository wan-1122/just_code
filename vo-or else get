
	
	/**
	 * <pre>
	 * {@code xxx.<Test, Integer>orElseGet(null
	    		, x -> {
	    			return x.test2;
	    		}, 11)
	     }
	 * </pre>
	 * @param class
	 * @param classifier
	 * @param defaultValue
	 * @return
	 */
	public static <E, V> V orElseGet(E class, Function<E, V> classifier, V defaultValue) {
		if (class == null) {
			return defaultValue;
		}
		return classifier.apply(class);
	}
  
  

enum value 에 대한 디폴트값을 빼내려고 만들었으나..

function만 만들 수 있으면 다 됨;
  
만들다 보니.. Function 이 열일;
